# lukast1997

This project is written in Java, using Selenium 3.9.0.
It was run by Eclipse IDE.
Eclipse must be set up with Selenium JAR files and also with Google Chrome driver.
It runs on main method.

Also in first line in main method you need to change the location of chromedriver.exe